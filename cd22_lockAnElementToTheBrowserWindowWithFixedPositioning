 #navbar {
    position: fixed;
    top: 0px;
    left: 0px;
    width: 100%;
    background-color: #767676;
  }
  
  //Fixed positioning locks an element to the browser window, and the element won't move when the user scrolls//
  //Other elements won't "know" where the element is, so they'll have to be adjusted manually to accommodate the location of it//
